html
  head
    title= title
  body
    h1= title

    form(action="/upload" method="post" enctype="multipart/form-data")
      input(type="file" name="myFile")
      button(type="submit") Upload Image
    br
    each image in images
      img(style="width: 400px" src="/uploads/" + image)
